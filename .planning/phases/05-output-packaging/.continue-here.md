---
phase: v2-twitter-linkedin
task: 6
total_tasks: 6
status: complete_pending_live_test
last_updated: 2026-02-08
---

<current_state>
Twitter + LinkedIn enrichment has been fully implemented in enrich_calendar.py.
All code is written and --dry-run verified. NOT yet tested with live API calls
(requires APIFY_TOKEN + OPENAI_API_KEY).
The code is uncommitted — sitting as a modified enrich_calendar.py on main.
</current_state>

<completed_work>

- Task 1: Extract shared helpers - Done (extract_keywords to module level, _style_sheet helper)
- Task 2: Twitter integration - Done (10 functions: queries, actor, normalize, score, fetch, process, select, LLM, enrich_row_twitter)
- Task 3: LinkedIn integration - Done (10 functions: queries, actor, normalize, score, fetch, process, select, LLM, enrich_row_linkedin)
- Task 4: Refactor write_enriched_excel - Done (3 sheets: TikTok, Twitter, LinkedIn)
- Task 5: Extend main() and build_run_log() - Done (per-platform loop, per-platform status counts)
- Task 6: Dry-run verification - Done (30 rows x 3 platforms, all 19 new functions import cleanly)
</completed_work>

<remaining_work>

- Live API test: Run `python enrich_calendar.py --input "Content ideas.xlsx"` with real API keys
- Verify Twitter sheet has draft tweets + thread structures + 3 example references per row
- Verify LinkedIn sheet has draft posts + 3 example references per row
- Verify run_log.json includes twitter/linkedin diagnostics per row
- Verify cost stays within ~$4-5 target for 30-row run
- Commit the changes
</remaining_work>

<decisions_made>

- Added all Twitter + LinkedIn code in single session, following exact TikTok patterns
- Twitter scoring: log10(likes+1)*0.45 + log10(retweets+1)*0.30 + log10(replies+1)*0.25
- LinkedIn scoring: log10(reactions+1)*0.40 + log10(comments+1)*0.35 + log10(shares+1)*0.25
- Twitter queries use hashtag + topic style, LinkedIn uses professional phrases
- LinkedIn actor uses searchUrl (constructed via _build_linkedin_search_url)
- Twitter actor uses searchTerms array with sort="Top" and tweetLanguage="en"
- Separate query caches per platform (_twitter_query_cache, _linkedin_query_cache)
- Per-platform error handling in main() — one platform failing doesn't block others
- build_run_log now has per-platform status counts (tiktok_status_counts, twitter_status_counts, linkedin_status_counts)
- Each row in run_log has tiktok/twitter/linkedin sub-objects
</decisions_made>

<blockers>
None. Code is complete, just needs live testing.
</blockers>

<context>
The v1 milestone (TikTok-only enrichment) was completed across 5 phases with 8 plans.
This session added Twitter + LinkedIn as a single implementation pass outside the GSD phase
structure, following the approved plan. The file grew from ~1540 to ~2300 lines.

Key new constants: TWITTER_ACTOR_ID, TWITTER_RESULTS_PER_QUERY (10), TWITTER_MAX_RESULTS_PER_ROW (30),
LINKEDIN_ACTOR_ID, LINKEDIN_RESULTS_PER_QUERY (10), LINKEDIN_MAX_RESULTS_PER_ROW (30).

The normalize functions handle multiple possible field names from the Apify actors since
the exact response schema hasn't been verified with live calls yet.
</context>

<next_action>
Run a live test: `python enrich_calendar.py --input "Content ideas.xlsx"`
If API response schemas don't match normalize functions, adjust field mappings.
Then commit the working code.
</next_action>
