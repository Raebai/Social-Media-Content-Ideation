---
phase: 02-query-api-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [enrich_calendar.py]
autonomous: true
user_setup:
  - service: apify
    why: "TikTok data fetching via clockworks/tiktok-scraper actor"
    env_vars:
      - name: APIFY_TOKEN
        source: "Apify Console -> Settings -> Integrations -> API tokens"

must_haves:
  truths:
    - "generate_queries() returns 3-6 queries per ContentIdea"
    - "At least one query per row is a format-specific query derived from Type mapping"
    - "All queries are <=5 words"
    - "Apify actor input/output schema is documented in summary for Plan 02-02"
  artifacts:
    - path: "enrich_calendar.py"
      provides: "generate_queries() function and TYPE_QUERY_MAP constant"
      contains: "def generate_queries"
  key_links:
    - from: "generate_queries()"
      to: "ContentIdea.idea_text"
      via: "Parses idea_text to extract keywords for query construction"
      pattern: "idea\\.idea_text|idea\\.content_type|idea\\.topic"
---

<objective>
Add query generation logic and discover Apify actor schema.

Purpose: Query generation (QRY-01, QRY-02) is the first half of Phase 2. The Apify schema discovery (API-04) is a prerequisite for Plan 02-02's integration code. Combining these in one plan ensures Plan 02-02 has both the query function and the schema knowledge it needs.

Output: `generate_queries()` function in enrich_calendar.py + documented Apify actor schema in summary.
</objective>

<execution_context>
@C:\Users\Raaed\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Raaed\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@enrich_calendar.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement generate_queries() with Type-based format queries</name>
  <files>enrich_calendar.py</files>
  <action>
Add the following to enrich_calendar.py, AFTER the existing code (do not modify existing functions):

1. Add a TYPE_QUERY_MAP constant dict mapping each content type to its format query string:
   - "Story" -> "founder story"
   - "BTS" -> "day in the life"
   - "Teach" -> "startup lesson"
   - "Trend" -> "new chapter"
   - "Breakdown" -> "mindset shift"
   - "Reflection" -> "founder reflection"
   - "Depth" -> "longform reflection"

2. Add function `generate_queries(idea: ContentIdea) -> List[str]`:
   - Extract keywords from idea.topic and idea.description (split on spaces, take unique meaningful words >3 chars, lowercase)
   - Build queries:
     a. Start with the format query from TYPE_QUERY_MAP[idea.content_type] (this satisfies QRY-02)
     b. Add 1-2 keyword combination queries (2-3 keywords joined, <=5 words each). Draw from topic first, then description.
     c. Add idea.topic as a query if it's <=5 words
     d. Add a niche query combining content_type keyword + top topic keyword (e.g., "startup founder journey")
   - Enforce constraints: each query <=5 words, total 3-6 queries, no duplicates
   - Return list of query strings

3. IMPORTANT: All queries must be <=5 words. Count words with `len(query.split())`. If any query exceeds 5 words, truncate to first 5 words.

4. IMPORTANT: Ensure minimum 3 queries. If fewer than 3 after dedup, add generic fallback queries like "startup founder content" or "entrepreneur tiktok".

Do NOT touch any existing functions. Add new code below the existing `print_summary()` function but above the `if __name__` block.
  </action>
  <verify>
Run: `python -c "from enrich_calendar import generate_queries, load_content_ideas, TYPE_QUERY_MAP; ideas, _ = load_content_ideas('Content ideas.xlsx'); queries = generate_queries(ideas[0]); print(f'Queries for row {ideas[0].row_number}: {queries}'); print(f'Count: {len(queries)}'); assert 3 <= len(queries) <= 6, f'Expected 3-6 queries, got {len(queries)}'; assert all(len(q.split()) <= 5 for q in queries), 'Query exceeds 5 words'; print('TYPE_QUERY_MAP keys:', list(TYPE_QUERY_MAP.keys())); print('PASS')"`

Also spot-check 3 different content types:
`python -c "from enrich_calendar import generate_queries, load_content_ideas, TYPE_QUERY_MAP; ideas, _ = load_content_ideas('Content ideas.xlsx'); [print(f'{i.content_type}: {generate_queries(i)}') for i in ideas[:5]]"`
  </verify>
  <done>
generate_queries() returns 3-6 queries per ContentIdea, each <=5 words, with at least one format-specific query from TYPE_QUERY_MAP matching the row's content type. TYPE_QUERY_MAP contains all 7 content types.
  </done>
</task>

<task type="auto">
  <name>Task 2: Discover Apify clockworks/tiktok-scraper actor schema</name>
  <files>enrich_calendar.py</files>
  <action>
This is a DISCOVERY task. The goal is to inspect the actual Apify actor schema so Plan 02-02 can write correct API integration code.

Steps:

1. Use the Apify REST API to get the actor's input schema. The endpoint is:
   `GET https://api.apify.com/v2/acts/clockworks~tiktok-scraper`
   This does NOT require authentication for public actors.

   Run via curl or Python requests:
   ```
   curl -s "https://api.apify.com/v2/acts/clockworks~tiktok-scraper" | python -m json.tool
   ```

2. Also fetch the actor's input schema specifically:
   ```
   curl -s "https://api.apify.com/v2/acts/clockworks~tiktok-scraper/input-schema" | python -m json.tool
   ```

3. If the above endpoints don't return the schema, try the Apify Store page:
   - Check `https://api.apify.com/v2/store?search=clockworks+tiktok-scraper`
   - Or try alternate actor ID formats

4. Document what you find:
   - What input fields does the actor accept? (searchQueries, hashtags, maxItems, etc.)
   - What is the correct input format for running the actor?
   - What fields does the actor output per result? (video URL, caption, author, view count, audio info, etc.)
   - What is the API endpoint for running the actor? (`POST /v2/acts/{actorId}/runs`)
   - How to retrieve results after a run completes?

5. CRITICAL: Record ALL findings in the plan summary. Plan 02-02 depends on this discovery. If the actor doesn't exist or has a different ID, document that too -- it changes the approach for Plan 02-02.

6. If the actor schema reveals that it uses `searchQueries` (array of strings) as input, that's the expected pattern. If it uses a different field name, note the exact field name.

7. Do NOT write any Apify integration code yet. Just discover and document. The only code change allowed in this task is: if you discover the exact package name needed (e.g., `apify-client` vs `requests`), note it for Plan 02-02.

NOTE: This task modifies enrich_calendar.py in the files list only because it shares the plan, but the actual file change is minimal or none. The primary output is KNOWLEDGE documented in the summary.
  </action>
  <verify>
Verify that:
1. The actor ID `clockworks/tiktok-scraper` resolves (or document the correct ID)
2. The input schema fields are documented
3. The output schema fields are documented (especially: does it include audio metadata?)
4. The API call pattern for running an actor and retrieving results is documented

If curl commands fail, try Python:
```python
import urllib.request, json
data = json.loads(urllib.request.urlopen("https://api.apify.com/v2/acts/clockworks~tiktok-scraper").read())
print(json.dumps(data, indent=2)[:3000])
```
  </verify>
  <done>
Apify actor schema is discovered and documented. The summary for this plan contains: (a) correct actor ID, (b) input field names and format, (c) output field names including audio metadata presence, (d) API call pattern for running and retrieving results. Plan 02-02 can proceed with this knowledge.
  </done>
</task>

</tasks>

<verification>
1. `generate_queries()` exists in enrich_calendar.py and is importable
2. TYPE_QUERY_MAP contains all 7 content types from VALID_TYPES
3. For every content type, at least one query matches the TYPE_QUERY_MAP entry
4. All queries are <=5 words
5. Query count per row is 3-6
6. Apify actor schema is documented in summary
7. Existing Phase 1 code (load_content_ideas, ContentIdea, etc.) is unchanged
</verification>

<success_criteria>
- generate_queries() returns 3-6 queries per row, each <=5 words
- At least one format-specific query per row from TYPE_QUERY_MAP
- Apify actor input/output schema fully documented for Plan 02-02
- All existing code untouched and still working
</success_criteria>

<output>
After completion, create `.planning/phases/02-query-api-integration/02-01-SUMMARY.md`

CRITICAL: The summary MUST include a "## Apify Schema Discovery" section with:
- Actor ID (confirmed or corrected)
- Input schema (field names, types, required/optional)
- Output schema (field names, especially audio metadata)
- API call pattern (endpoint, method, how to retrieve results)
- Any surprises or deviations from expected behavior

This section is the PRIMARY input for Plan 02-02.
</output>
