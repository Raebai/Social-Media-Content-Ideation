---
phase: 02-query-api-integration
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [enrich_calendar.py, requirements.txt]
autonomous: true
user_setup:
  - service: apify
    why: "TikTok data fetching -- APIFY_TOKEN must be set in environment"
    env_vars:
      - name: APIFY_TOKEN
        source: "Apify Console -> Settings -> Integrations -> API tokens"

must_haves:
  truths:
    - "fetch_tiktok_results() calls Apify actor and returns raw result dicts"
    - "Failed API calls are retried up to 3 times with exponential backoff"
    - "Identical queries within a run return cached results (no duplicate API calls)"
    - "Total results per row are capped at 150 regardless of query count"
    - "requests library is added to requirements.txt"
  artifacts:
    - path: "enrich_calendar.py"
      provides: "fetch_tiktok_results() function with retry, caching, and cap logic"
      contains: "def fetch_tiktok_results"
    - path: "requirements.txt"
      provides: "Updated dependency list including requests"
      contains: "requests"
  key_links:
    - from: "fetch_tiktok_results()"
      to: "generate_queries()"
      via: "Takes query list from generate_queries() as input"
      pattern: "generate_queries|queries"
    - from: "fetch_tiktok_results()"
      to: "Apify REST API"
      via: "HTTP POST to run actor, GET to retrieve results"
      pattern: "api\\.apify\\.com"
---

<objective>
Implement Apify TikTok scraper integration with retry logic and caching.

Purpose: This is the second half of Phase 2, covering API-01 through API-06. It uses the actor schema discovered in Plan 02-01 to build the correct API integration. The function `fetch_tiktok_results()` will be the bridge between query generation and data processing (Phase 3).

Output: `fetch_tiktok_results()` function in enrich_calendar.py + updated requirements.txt with requests.
</objective>

<execution_context>
@C:\Users\Raaed\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Raaed\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@enrich_calendar.py
@.planning/phases/02-query-api-integration/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement fetch_tiktok_results() with retry, caching, and cap</name>
  <files>enrich_calendar.py, requirements.txt</files>
  <action>
FIRST: Read `02-01-SUMMARY.md` to get the Apify actor schema discovery results. The input field names, output field names, and API call pattern are documented there. ADAPT your implementation based on what was discovered. Do NOT hardcode assumed field names -- use what the schema actually says.

Add to enrich_calendar.py (below generate_queries(), above `if __name__` block):

1. Add `import requests`, `import time`, `import os`, `import hashlib`, `import json` at the top of the file (with the other imports).

2. Add module-level constants:
   - `APIFY_BASE_URL = "https://api.apify.com/v2"`
   - `APIFY_ACTOR_ID = "clockworks~tiktok-scraper"` (use tilde format for API calls, or adjust based on 02-01 discovery)
   - `MAX_RESULTS_PER_ROW = 150`
   - `RESULTS_PER_QUERY = 25` (middle of 20-30 range)
   - `MAX_RETRIES = 3`

3. Add a simple cache class or module-level dict:
   ```python
   _query_cache: Dict[str, List[Dict]] = {}
   ```
   Cache key: the query string itself (lowercased, stripped). Before making an API call, check if the query is already in cache. After a successful call, store results in cache. (API-06)

4. Add function `_run_apify_actor(query: str, token: str, max_items: int = RESULTS_PER_QUERY) -> List[Dict[str, Any]]`:
   - Build input payload based on discovered schema (from 02-01-SUMMARY.md). Expected pattern:
     - POST `{APIFY_BASE_URL}/acts/{APIFY_ACTOR_ID}/runs?token={token}&waitForFinish=300`
     - Body: JSON with search query and max items (exact field names from discovery)
   - Alternatively, if the actor supports synchronous runs:
     - POST `{APIFY_BASE_URL}/acts/{APIFY_ACTOR_ID}/run-sync-get-dataset-items?token={token}`
   - Parse response and return list of result dicts
   - If the actor run is async, poll for completion:
     - GET `{APIFY_BASE_URL}/actor-runs/{runId}?token={token}` until status is SUCCEEDED
     - Then GET `{APIFY_BASE_URL}/actor-runs/{runId}/dataset/items?token={token}`
   - IMPORTANT: Adapt field names and endpoint patterns based on what 02-01 discovered

5. Add function `_call_with_retry(fn, *args, max_retries=MAX_RETRIES, **kwargs) -> Any`:
   - Try calling fn(*args, **kwargs) up to max_retries times
   - On failure (requests.RequestException or HTTP 4xx/5xx), wait with exponential backoff: `time.sleep(2 ** attempt)` (so 1s, 2s, 4s)
   - On final failure, return empty list (don't crash the whole pipeline)
   - Log each retry attempt to stderr with `print(f"Retry {attempt}/{max_retries} for ...", file=sys.stderr)` (add `import sys` at top)

6. Add function `fetch_tiktok_results(queries: List[str], token: Optional[str] = None) -> List[Dict[str, Any]]`:
   - If token is None, get from `os.environ.get("APIFY_TOKEN")`
   - If still None, raise ValueError("APIFY_TOKEN not found in environment")
   - Initialize results list
   - For each query in queries:
     - Check cache first (API-06). If hit, extend results with cached data and continue
     - Call `_call_with_retry(_run_apify_actor, query, token)`
     - Store results in cache
     - Extend results list
     - Check if total results >= MAX_RESULTS_PER_ROW (API-03). If so, truncate to MAX_RESULTS_PER_ROW and break
   - Return results (capped at MAX_RESULTS_PER_ROW)

7. Update requirements.txt: Add `requests>=2.28.0` on a new line.

IMPORTANT NOTES:
- Do NOT modify any existing functions from Phase 1
- The function should work correctly even if APIFY_TOKEN is not set (it raises a clear error)
- Use `requests` library, NOT `urllib` (per project constraints saying "requests")
- All API calls go through the retry wrapper
  </action>
  <verify>
1. Verify imports work:
   `python -c "from enrich_calendar import fetch_tiktok_results, generate_queries, _query_cache; print('Imports OK')"`

2. Verify existing Phase 1 code still works:
   `python -c "from enrich_calendar import load_content_ideas; ideas, _ = load_content_ideas('Content ideas.xlsx'); print(f'Loaded {len(ideas)} ideas - Phase 1 still works')"`

3. Verify APIFY_TOKEN validation:
   `python -c "from enrich_calendar import fetch_tiktok_results; import os; os.environ.pop('APIFY_TOKEN', None); try: fetch_tiktok_results(['test']); except ValueError as e: print(f'Correct error: {e}')"`

4. Verify requirements.txt has requests:
   `python -c "open('requirements.txt').read().index('requests'); print('requirements.txt updated')"`

5. Verify constants exist:
   `python -c "from enrich_calendar import MAX_RESULTS_PER_ROW, RESULTS_PER_QUERY, MAX_RETRIES; print(f'Cap: {MAX_RESULTS_PER_ROW}, Per query: {RESULTS_PER_QUERY}, Retries: {MAX_RETRIES}')"`
  </verify>
  <done>
fetch_tiktok_results() is implemented with: Apify actor calls using discovered schema, 3-attempt exponential backoff retry, per-query caching, 150 result cap per row. requirements.txt includes requests. All Phase 1 code untouched and working.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify end-to-end query-to-fetch pipeline structure</name>
  <files>enrich_calendar.py</files>
  <action>
Wire the full Phase 2 pipeline and verify it structurally (without requiring a live APIFY_TOKEN).

1. Add a helper function `enrich_row_queries(idea: ContentIdea) -> Dict[str, Any]` that:
   - Calls `generate_queries(idea)` to get queries
   - Returns a dict with: `{"row_number": idea.row_number, "queries": queries, "query_count": len(queries)}`
   - This is a structural helper that Phase 3+ will extend

2. Update the `if __name__ == "__main__"` block to demonstrate the Phase 2 pipeline:
   - Load ideas with `load_content_ideas()`
   - Print summary (existing)
   - For each idea, call `enrich_row_queries()` and print the queries
   - Print total query count across all rows
   - Add a note: "Set APIFY_TOKEN to fetch TikTok results"
   - Do NOT actually call fetch_tiktok_results() in the main block (no token available during dev)

3. Verify that for ALL 30 rows:
   - generate_queries() returns 3-6 queries each
   - At least one query matches TYPE_QUERY_MAP[idea.content_type]
   - No query exceeds 5 words

Run full verification across all rows, not just a sample.
  </action>
  <verify>
Run the full pipeline:
`python enrich_calendar.py`

Expected output should show:
- "Loaded 30 content ideas" (from Phase 1)
- Query listings for each row showing 3-6 queries each
- Summary of total queries generated

Also run a comprehensive check:
```
python -c "
from enrich_calendar import load_content_ideas, generate_queries, TYPE_QUERY_MAP
ideas, _ = load_content_ideas('Content ideas.xlsx')
errors = []
for idea in ideas:
    queries = generate_queries(idea)
    if not (3 <= len(queries) <= 6):
        errors.append(f'Row {idea.row_number}: {len(queries)} queries (expected 3-6)')
    if not all(len(q.split()) <= 5 for q in queries):
        long = [q for q in queries if len(q.split()) > 5]
        errors.append(f'Row {idea.row_number}: queries too long: {long}')
    format_q = TYPE_QUERY_MAP[idea.content_type]
    if format_q not in queries:
        errors.append(f'Row {idea.row_number}: missing format query \"{format_q}\"')
if errors:
    print('ERRORS:')
    for e in errors: print(f'  {e}')
else:
    print(f'ALL {len(ideas)} rows passed: 3-6 queries, <=5 words, format query present')
"
```
  </verify>
  <done>
End-to-end pipeline verified: all 30 rows generate 3-6 queries each, all queries <=5 words, all rows include format-specific query, main block demonstrates the full Phase 2 pipeline. enrich_row_queries() helper ready for Phase 3 extension.
  </done>
</task>

</tasks>

<verification>
1. fetch_tiktok_results() exists and is importable
2. Retry logic attempts up to 3 times with exponential backoff
3. Cache prevents duplicate API calls for same query string
4. Results capped at 150 per row
5. APIFY_TOKEN validated from environment
6. requirements.txt contains requests>=2.28.0
7. All Phase 1 code untouched (load_content_ideas, ContentIdea, etc.)
8. generate_queries() still works correctly for all 30 rows
9. enrich_row_queries() helper wires queries to row context
</verification>

<success_criteria>
- fetch_tiktok_results() handles Apify API calls with correct input format from discovered schema
- 3-attempt exponential backoff retry on failures
- Per-query caching avoids duplicate API calls
- 150 result cap enforced per row
- requirements.txt updated with requests
- Full pipeline structurally verified across all 30 rows
</success_criteria>

<output>
After completion, create `.planning/phases/02-query-api-integration/02-02-SUMMARY.md`

Summary should document:
- fetch_tiktok_results() API call pattern (endpoints, payload format)
- Retry and caching behavior
- Any adaptations made based on Apify schema discovery from 02-01
- Readiness for Phase 3 (what data structure Phase 3 will receive)
</output>
